#include<iostream>
#include <vector>
#include <algorithm>
#include <numeric>
using namespace std;

int main(){

    //vector <int> v;                   // empty vector
    //vector <int> v(n);               //vector with fixed size
    //vector <int> v(5);              //vector with fixed size
    //vector <int> v(n,0);           //vector same with same value
    //vector <int> v(5,15);         //vector with fixed saze ande same initialize value for example 15,15,15,15,15
    //vector <int> v={1,2,3,4,5};  //initialize with values

    //vector<int> v1 = {1,2,3,4,5}; 
    //vector<int> v2(v1);                                   //copy another vector

    //vector<int> v1 = {1,2,3,4,5}; 
    //vector<int> v2 = (v1);                              // copy using assignment

    //vector<int> v1 = {1,2,3,4,5}; 
    //vector<int> v2(v1.begin(),v1.end());             //Create from a range (iterator)

    //vector <vector<int>> matrix;                    // 2D vector
    //vector<vector<int>> matrix(3, vector<int>(4)); //  2D vector with size


    //vector<vector<vector<int>>> cube;            //3D vector
    //vector<vector<vector<int>>> cube(
    //2,
    //vector<vector<int>>(3, vector<int>(4))     //3D vector with size
//);

    //vector<pair<int, int>> vp;                //vector of pairs

    //vector <string> names;                   //vector of string

    //vector <bool> visited(100,false);

}